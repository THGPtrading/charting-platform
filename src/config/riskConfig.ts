// src/config/riskConfig.ts
export const riskConfig = {
  riskPercent: 0.10,        // 10% of investment (adjustable later)
  defaultInvestment: 10000, // baseline investment amount
  defaultRatio: 2.5         // default risk/reward ratio (can be 2.5, 3, etc.)
};
